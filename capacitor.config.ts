import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.currencyexchange.app',
  appName: 'Currency Exchange',
  webDir: 'dist/currency-exchange-app/browser',
};

export default config;
